name: Render Remotion Video

on:
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  render:
    runs-on: ubuntu-latest
    steps:
      - name: Verify Node 24 action migration
        run: |
          echo "FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=$FORCE_JAVASCRIPT_ACTIONS_TO_NODE24"
          if grep -R -nE "actions/(checkout|upload-artifact)@v4" .github/workflows; then
            echo "Found deprecated action version(s)."
            exit 1
          fi

      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm install

      - name: Render AILiteracyCourse composition to MP4
        run: npm run build

      - name: Upload MP4 artifact
        uses: actions/upload-artifact@v5
        with:
          name: ai-literacy-video
          path: public/ai-literacy-course.mp4
          if-no-files-found: error
